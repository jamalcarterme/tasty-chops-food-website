/* ===================================================
   Tasty Chops – API configuration
   =================================================== */

// Backend root — the /api prefix is added per-request in auth.js, cart.js etc.
// If your backend routes are at /auth/signup (no /api), change to:
//   const API_BASE = 'https://tasty-chops-backend.onrender.com';
const API_BASE = 'https://tasty-chops-backend.onrender.com/api';

const WHATSAPP_NUMBER = '+2348032056675';
